'use strict';

/**
 * sandwiches-slider controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::sandwiches-slider.sandwiches-slider');
